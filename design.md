# เอกสารออกแบบ — Solar MPPT Boost Charge Controller

ESP32 + Boost converter + P&O MPPT
แผง 20W · Vmp ~12V · Imp 1.11A · Voc วัดจริง ~15V · Isc 1.22A → แบต SLA 12V 7Ah
ฟิวส์ฝั่งแผง **2A** · ฟิวส์ฝั่งแบต **3A** · สวิตช์ **50 kHz** · duty ที่จุดทำงาน ≈ **0.17**

---

# 1. ขาของ IR2104

ยืนยันจาก Lead Assignment ในดาต้าชีต Infineon PD60046-P

| ขา | ชื่อ | หน้าที่ | ต่ออะไร |
|---|---|---|---|
| 1 | VCC | ไฟเลี้ยง 10–20V | R 10Ω จาก NODE IN |
| 2 | IN | อินพุตเดียว คุมทั้งสองฝั่ง | ESP32 GPIO25 |
| 3 | SD | ปิดการทำงาน (active low) | ESP32 GPIO26 + R 10k ลง GND |
| 4 | COM | กราวด์ | GND bus |
| **5** | **LO** | เอาต์พุตฝั่งต่ำ | R 10Ω → Q1 Gate |
| **6** | **VS** | ฐานอ้างอิงฝั่งสูง | GND bus |
| **7** | **HO** | เอาต์พุตฝั่งสูง | **ปล่อยลอย ห้ามต่ออะไร** |
| **8** | **VB** | ไฟลอยฝั่งสูง | **ต่อไปขา 1** + C 0.1µF ลงขา 6 |

**ขา 5 และ ขา 7 เป็นเอาต์พุตทั้งคู่** ต่อลงกราวด์หรือต่อเข้าไฟเลี้ยงเมื่อไหร่ IC พังทันที

## ค่าจากดาต้าชีตที่มีผลกับงานนี้

| ค่า | ตัวเลข | ความหมายกับวงจรนี้ |
|---|---|---|
| VCC ที่แนะนำ | 10–20V | แผง 12–16V อยู่ในช่วงพอดี ไม่ต้องใช้ 7812 |
| VCC UVLO ขาขึ้น | 8.9V typ (สูงสุด 9.8V) | แสงน้อยจนแผงต่ำกว่า ~10V ไดรเวอร์ปิดเอง แล้วแผงเด้งกลับ Voc — กลับมาเองได้ |
| VIH ขั้นต่ำ | 3.0V | ESP32 ให้ 3.3V เหลือมาร์จิ้น 0.3V |
| Deadtime | 400–650ns | กินไป ~2.6% ของคาบที่ 50kHz · duty ต่ำกว่า ~0.03 จะไม่ออกเลย |
| IO+ / IO− | 130mA / 270mA | เกตเปิด ~214ns ปิด ~125ns |
| HO อยู่ในเฟสเดียวกับ IN | — | **LO จึงกลับเฟสกับ IN** → ในโค้ดต้องเขียน (1 − duty) |

---

# 2. เส้นทางกระแส

```
TB1(+) → F1 2A → [NODE IN] → INA1 VIN+
                              INA1 VIN− → [NODE CIN] → Cin(+) → L1 ขาเข้า
L1 ขาออก → [NODE SW] → Q1 Drain
                     → D1 Anode (ขา 1+3)
Q1 Source → GND (power)
D1 Cathode (ขากลาง+tab) → [NODE OUT] → Cout(+) → INA2 VIN+
INA2 VIN− → TB2 Battery(+)
```

---

# 3. ตารางขาต่อขา

## กลุ่ม A — ไฟเข้า

| อุปกรณ์ | ขา | ต่อไปที่ |
|---|---|---|
| **TB1 Solar** | (+) | F1 ขาเข้า |
| | (−) | GND bus (power) |
| **F1 ฟิวส์ 2A** | ขาเข้า | TB1 (+) |
| | ขาออก | **NODE IN** |

> **NODE IN** แตก 3 ทาง: INA1 VIN+ · MP1584 IN(+) · R 10Ω ไปขา 1 ของ IR2104

## กลุ่ม B — Power stage

| อุปกรณ์ | ขา | ต่อไปที่ |
|---|---|---|
| **INA1 (0x40)** | VIN+ | NODE IN |
| | VIN− | **NODE CIN** |
| | VCC / GND | 3.3V / GND bus |
| | SDA / SCL | I2C bus |
| **Cin 470µF/25V** | (+) | NODE CIN |
| | (−) | GND bus (power) |
| **C 0.1µF เซรามิก** | คร่อม | NODE CIN ↔ GND |
| **L1 100µH** | ขาเข้า | NODE CIN |
| | ขาออก | **NODE SW** |
| **Q1 IRF3205** | G (ซ้าย) | R 10Ω → IR2104 ขา 5 · และ R 10k → GND |
| | D (กลาง + tab) | NODE SW |
| | S (ขวา) | GND bus (power) |
| **D1 MBR20100CT** | ขา 1 + ขา 3 = Anode | NODE SW |
| | ขา 2 กลาง + tab = Cathode | **NODE OUT** |
| **Cout 1000µF/25V** | (+) | NODE OUT |
| | (−) | GND bus (power) |
| **C 0.1µF เซรามิก** | คร่อม | NODE OUT ↔ GND |
| **INA2 (0x41)** | VIN+ | NODE OUT |
| | VIN− | TB2 (+) |
| | VCC / GND | 3.3V / GND bus |
| **TB2 Battery** | (+) | INA2 VIN− |
| | (−) | GND bus (power) |

### จุดรวม

| โหนด | ขาที่ชนกัน |
|---|---|
| NODE CIN | INA1 VIN− · Cin(+) · C 0.1µF · L1 ขาเข้า |
| **NODE SW** | L1 ขาออก · Q1 Drain · D1 ขา1+ขา3 — **สายสั้นที่สุด** |
| NODE OUT | D1 ขากลาง · Cout(+) · C 0.1µF · INA2 VIN+ |

> Q1 tab = NODE SW · D1 tab = NODE OUT — คนละโหนด **ต้องมีแผ่นฉนวนไมก้า** ถ้าฮีตซิงก์แตะกัน

## กลุ่ม C — Gate driver

| อุปกรณ์ | ขา | ต่อไปที่ |
|---|---|---|
| **R 10Ω (Rvdd)** | คร่อม | NODE IN ↔ IR2104 ขา 1 |
| **C 10µF/25V** | (+) / (−) | ขา 1 / GND bus |
| **C 0.1µF** | คร่อม | ขา 1 ↔ GND bus (ติดชิด IC) |
| **IR2104 ขา 8 (VB)** | — | **ต่อตรงไป ขา 1** |
| **C 0.1µF** | คร่อม | ขา 8 ↔ ขา 6 |
| **IR2104 ขา 6 (VS)** | — | GND bus (signal) |
| **IR2104 ขา 7 (HO)** | — | **ปล่อยลอย** |
| **R 10Ω (Rgate)** | คร่อม | ขา 5 (LO) ↔ Q1 Gate |
| **R 10kΩ (Rpd)** | คร่อม | Q1 Gate ↔ GND |
| **R 10kΩ (Rsd)** | คร่อม | ขา 3 (SD) ↔ GND |

> **R 10k ที่ SD คือระบบนิรภัยเดียวของบอร์ดนี้** ขา IN ไม่มีตัวดึงในตัว ถ้า ESP32 ยังไม่บูตหรือค้าง ขา IN จะลอย ซึ่งอาจถูกอ่านเป็น LOW แล้ว LO จะเป็น HIGH ทำให้ Q1 ค้างเปิด ตัวที่กันไว้คือ SD ที่ถูกดึงต่ำ **ห้ามละเว้นตัวนี้**

## กลุ่ม D — ควบคุม

| อุปกรณ์ | ขา | ต่อไปที่ |
|---|---|---|
| **MP1584** | IN(+) / IN(−) | NODE IN / GND bus |
| | OUT(+) | จั๊มเปอร์เลือกแหล่งจ่าย → ESP32 VIN |
| | OUT(−) | GND bus |
| **ESP32** | D25 | IR2104 ขา 2 (IN) |
| | D26 | IR2104 ขา 3 (SD) |
| | D27 | รีเลย์ (ทางเลือก ดูหัวข้อ 7.2) |
| | D21 / D22 | I2C SDA / SCL |
| | 3V3 | รางจ่าย 3.3V |
| **OLED (0x3C)** | VCC / GND / SDA / SCL | 3.3V / GND / I2C bus |

> บนบอร์ด ESP32 DevKit ตัวอักษร "D" เป็นแค่คำนำหน้า เลขหลัง D คือเลข GPIO ตรง ๆ

### หมายเหตุเรื่อง MP1584

โมดูลที่ใช้ (ป้าย V752-755) เป็นรุ่น **เอาต์พุตคงที่ ไม่มีสกรูปรับ** แรงดันถูกกำหนดด้วยตัวต้านทานป้อนกลับบนโมดูล วัดได้ราว 5.22V ซึ่งอยู่ในช่วงที่ขา VIN ของ ESP32 รับได้

เนื่องจากบอร์ด DevKit ต่อ USB 5V เข้าขา VIN โดยไม่มีไดโอดกั้น จึงเพิ่ม **จั๊มเปอร์ 2 ขาคั่นระหว่าง MP1584 OUT+ กับ VIN** เพื่อให้แน่ใจว่า USB กับแหล่งจ่ายภายนอกไม่ถูกต่อพร้อมกัน

---

# 4. จุดลง GND

**ราง POWER** — TB1(−) · Cin(−) · **Q1 Source** · Cout(−) · TB2(−) · เซรามิก 0.1µF ทั้งสองตัวของโหนดกำลัง

**ราง SIGNAL** — INA1 GND · INA2 GND · IR2104 ขา 4 และ ขา 6 · R 10k ทั้งสองตัว · C 10µF และ 0.1µF ของไฟเลี้ยง · MP1584 IN(−)/OUT(−) · ESP32 GND · OLED GND

**เชื่อมสองรางที่จุดเดียว** ใกล้ Cin(−)/Cout(−) ที่สุด

> Q1 Source คือจุดที่กระแสพัลส์ลงกราวด์ ต้องอยู่บนราง power และใกล้ Cin(−) กับ Cout(−) มากที่สุด ถ้าเดินยาวบอร์ดจะรวน

---

# 5. การคำนวณกำลังสูญเสีย

| จุด | กำลังที่เสีย | หมายเหตุ |
|---|---|---|
| D1 สวิตชิ่ง | **0.42W** | ตัวที่ร้อนที่สุด → ใส่ฮีตซิงก์ตัวเดียวที่นี่ |
| ชันต์ INA219 ทั้งสองตัว | 0.21W | 1.6% ของ 13.3W — ยอมรับได้ |
| Q1 สวิตชิ่ง | 0.14W | เกตเปิด 214ns ปิด 125ns |
| Q1 นำกระแส | 0.002W | Rds(on) 8mΩ แทบไม่มีผล |

**Q1 ไม่ต้องใช้ฮีตซิงก์** รวมแล้วไม่ถึง 0.15W

**ค่าอื่นที่ตรวจแล้ว**

- ริปเปิลใน L1 = 0.40A p-p บนกระแสเฉลี่ย 1.11A → อยู่ในโหมด CCM
- กระแสริปเปิล RMS ที่ Cout = 0.42A → ตัว 1000µF ทั่วไปรับได้
- กระแสพีคใน L1 = 1.3A จากพิกัด 10A → ไม่มีทางอิ่มตัว
- Vds สูงสุดของ Q1 ≈ 15V จากพิกัด 55V

---

# 6. สองเรื่องที่ต้องระวังที่สุด

## 6.1 ต่อขั้วกลับ = พังทันที

Cin, Cout เป็นอิเล็กโทรไลต์ ถ้าต่อแผงหรือแบตกลับขั้ว ตัวเก็บประจุจะโดนไบแอสย้อนแล้วระเบิด และ INA219 พังตาม

**ทุกครั้งก่อนต่อ** ตั้งมิเตอร์ที่ย่าน 20V DC วัดขั้วให้เห็นเลขบวกก่อนเสมอ สายแผงกับสายแบตมักไม่มีป้ายชัด

## 6.2 Boost ตัดวงจรเองไม่ได้

เมื่อ Q1 ปิดสนิท กระแสยังไหลผ่าน L1 → D1 → แบตอยู่ดี ได้แรงดันราว Voc − 0.5V ≈ **15V** ซึ่งสูงกว่าแรงดัน absorb 14.2V

ผลคือต่อให้โค้ดสั่งตัด แบตก็ยังถูกชาร์จต่อ ถ้าปล่อยทิ้งไว้หลายวันจะ overcharge

**ทางแก้ — เพิ่มรีเลย์ 5V หนึ่งตัว**

ต่ออนุกรมในสาย **แผง (+)** ระหว่าง F1 กับ NODE IN คุมด้วย GPIO27
เมื่อแบตเต็มหรือเกิด fault ให้ตัดรีเลย์ = ตัดต้นทางทั้งหมด

ต้องต่อฝั่งแผง ไม่ใช่ฝั่งแบต เพราะตัดฝั่งแบตขณะกำลังสวิตช์จะทำให้แรงดันเอาต์พุตพุ่ง

เฟิร์มแวร์รองรับแล้ว เปิดใช้ด้วยการเอาคอมเมนต์ออกที่บรรทัด `#define USE_RELAY`

ถ้าไม่ใส่รีเลย์ก็ใช้งานได้ แต่**อย่าปล่อยชาร์จทิ้งไว้ข้ามคืนโดยไม่ดู**

---

# 7. ลำดับประกอบและจุดตรวจ

| ขั้น | ทำ | ตรวจก่อนไปต่อ |
|---|---|---|
| 1 | GND bus สองราง + จุดเชื่อม | วัดความต่อเนื่องทั่วราง |
| 2 | TB1 · ฐานฟิวส์ · TB2 | — |
| 3 | INA1 → Cin → L1 → NODE SW → Q1/D1 → Cout → INA2 | ไม่มีจุดใดช็อตลง GND · วัดโหมดไดโอดยืนยันว่าขากลางของ D1 เป็นแคโทดจริง |
| 4 | MP1584 + R10Ω + C10µF + C0.1µF | **จ่าย 12V เข้า TB1 → MP1584 ต้องได้ ~5.2V · ขา 1 IR2104 ต้องได้ 11.8–12V** |
| 5 | Socket + R gate + R pull-down ×2 + C 0.1µF (VB-VS) | **ขา 7 ไม่ต่อกับอะไรเลย · ขา 8 ต่อถึงขา 1 · ขา 5 ไม่ต่อ GND** |
| 6 | เสียบ IR2104 · เสียบ ESP32 · I2C · OLED | I2C scan ต้องเจอ 0x40, 0x41, 0x3C |
| 7 | ไม่ต่อแบต ต่อโหลดจำลอง สั่ง duty คงที่ 0.10 | วัดแรงดันเฉลี่ยที่ Gate ต้องได้ ~1.2V |
| 8 | ต่อแบต เปิด MPPT | ดู log CSV ทาง serial |

**แหล่งจ่ายทดสอบ** boost ต้องการอินพุตต่ำกว่าเอาต์พุต อะแดปเตอร์ 12V ใช้ได้พอดี

รายละเอียดเต็มอยู่ใน `test_plan.md`

---

# 8. ทางเลือกเสริม

- **RC snubber คร่อม D1** — R 10Ω อนุกรม C 1nF ลดการแกว่งที่ NODE SW ถ้าเจอสัญญาณรบกวนกวน I2C
- **74HCT14 คั่นระหว่าง GPIO25 กับขา 2** — ถ้า duty กระตุกเพราะมาร์จิ้น 0.3V ไม่พอ
